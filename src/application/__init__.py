"""Application services layer.

Intentionally empty at Stage 0/1. This is where use-case orchestration
(e.g. "start a task", "advance a turn") will live once the agent loop is
built on top of the core domain foundation. Populating it now would be
scope creep -- the director's brief is explicit that Stage 0/1 is the
durable-state foundation, not the agent loop itself.
"""
